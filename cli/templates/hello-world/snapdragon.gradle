// Custom SnapDragon stuff. For when we rebuild the template 
// and update libGDX to the (future) latest version.
project(":core") {
    dependencies {
        compile "org.python:jython-standalone:2.7.1b2"
    }
}